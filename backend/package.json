{
  "name": "lchat-app",
  "version": "1.0.0",
  "description": "Complete cross-platform chat application with translation features",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "seed": "cd backend && node seed/seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["chat", "translation", "react-native", "nodejs", "mongodb"],
  "author": "LChat Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}